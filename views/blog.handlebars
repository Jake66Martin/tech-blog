  <section class = "section">
      <div class = "card">
        <div class = "card-title background">
          <h1 class = 'color'>{{newData.title}}</h1><p class = 'color'>Created by {{newData.user.username}} on {{date}}</p>
        </div>
        <div class = "card-content">
          <p>{{newData.content}}</p>
        </div>
      </div>
      <div>
      <div>
        {{#each comments}}
      <div class="card">
        <div class="card-title background">
          <p class = 'color'>Created by {{this.user.username}} on {{@root.date}}</p>
        </div>
        <div class="card-content">
          <p>{{this.comment}}</p>
        </div>
      </div>
    {{/each}}
      </div>
      <form class = 'sub-btn'>
        <div class = "comment-form">
          <div>
        <textarea class = "text" id = "comment" rows="7" cols="65">
          
        </textarea>
        </div>
        <div>
        <button type = "submit">Submit comment</button>
        </div>
        </div>
      </form>
     <script src="../js/newcomment.js"></script>

  </section>
